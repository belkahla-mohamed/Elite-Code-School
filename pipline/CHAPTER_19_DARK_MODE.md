# Chapter 19: Dark Mode & Theming

> **Goal**: Add dark mode toggle with CSS variables, persist preference in localStorage.

---

## Tasks

### [ ] T19.1 — CSS variables for dark mode
- Define dark palette in `globals.css`
- All colors as CSS variables
- `data-dark` attribute on `<html>`

### [ ] T19.2 — Theme toggle component
- Sun/Moon icon button in header
- Toggle between light/dark/system
- Persist choice in localStorage

### [ ] T19.3 — Dark mode audit
- Check all pages render correctly in dark mode
- Fix contrast issues
- Test form inputs, cards, buttons

### [ ] T19.4 — Smooth transitions
- CSS transition on `background-color` and `color`
- No flash on page load
- Respect `prefers-color-scheme`

---

**Progress**: `0 / 4 tasks completed`

**Next**: Chapter 20 — Export & Analytics
